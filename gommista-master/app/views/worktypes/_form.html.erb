<%= form_for(@worktype) do |f| %>
  <% if @worktype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@worktype.errors.count, "error") %> prohibited this worktype from being saved:</h2>

      <ul>
      <% @worktype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :work_id %><br>
    <%= f.text_field :work_id %>
  </div>
  <div class="field">
    <%= f.label :worktypolgy %><br>
    <%= f.text_field :worktypolgy %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

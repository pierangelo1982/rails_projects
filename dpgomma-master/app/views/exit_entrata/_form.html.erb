<%= simple_form_for(@exit_entratum) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :exit %>
    <%= f.association :entratum %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
